export enum StoryVisibility {
    PUBLIC = 'public', // Visible to everyone
    PRIVATE = 'private', // Only visible to author
    UNLISTED = 'unlisted', // Not listed publicly but accessible via direct link
}
